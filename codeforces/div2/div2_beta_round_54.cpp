#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

// A - Chat room
/*int32_t main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
 
    string s,obj{"hello"};
    cin >> s;
    size_t i{0},j{0};
    ll count{0};
    while(i<s.size() && j<obj.size()){
        if(obj[j]==s[i]){
            ++j;
            ++count;
        }
        ++i;
    }
    if(count==5) cout << "YES\n";
    else cout << "NO\n";
    return 0;
}*/

