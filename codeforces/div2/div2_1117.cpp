#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

// A 
/*int32_t main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    ll _{0};
    cin>>_;
    while(_--){
        ll n{0},m{0};
        cin>>n>>m;
        set<char> w,a;
        for(ll i{0};i<n;++i){
            string x;
            cin>>x;
            w.insert(x[0]-32);
        }
        for(ll i{0};i<m;++i){
            string x;
            cin>>x;
            for(auto i:x){
                a.insert(i);
            }
        }
        if(w==a) cout<<"YES\n";
        else cout<<"NO\n";
    }
    return 0;
}*/
