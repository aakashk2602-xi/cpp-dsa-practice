#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

// A - Three Numbers on the Blackboard
/*int32_t main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    ll _{0};
    cin >> _;
    while (_--) {
        vector<ll>v(3,0);
        cin >>v[0]>>v[1]>>v[2];
        sort(v.begin(),v.end());
        if(v[0]+v[1]<v[2]) cout<<v[1];
        else cout<<v[2]-v[0];
        cout<<"\n";
    }
    return 0;
}*/

// B - 