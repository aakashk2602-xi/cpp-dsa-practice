#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

// A 
/*int32_t main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    ll _{0};
    cin>>_;
    while(_--){
        ll k{0},x{0};
        cin>>k>>x;
        cout<<k*x+1<<"\n";
    }
    return 0;
}*/

// C
/*int32_t main(){
    ll _{0};cin>>_;
    while(_--){
        ll n{0};cin>>n;
        vector<ll> v(n,0);
        for(ll i{0};i<n;i++) cin>>v[i];
        sort(v.begin(),v.end());
        cout<<max(v[0],v[1]-v[0])<<'\n';
    }
}*/
