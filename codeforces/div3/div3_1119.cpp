#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
 
// A - Moo Language School
/*int32_t main(){
    ll _{0};cin>>_;
    while(_--){
        ll n{0},k{0};cin>>n>>k;
        string s;cin>>s;
        ll ans{0};
        for(ll i{0};i<n;i+=k){
            bool flag{false};
            for(ll j{i};j<i+k;++j){
                if(s[j]=='0') {flag=true;}
            }
            if(!flag){ans++;}
        }
        cout<<ans<<"\n";
    }
    return 0;
}*/

// B - Minus Two
