#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

// A - Beautiful Average 
/*int32_t main(){
    ll _{0};cin>>_;
    while(_--){
        ll n{0};cin>>n;
        ll avg{-1};ll x{0};
        for(ll i{0};i<n;++i){
            cin>>x;
            avg=max(avg,x);
        }
        cout<<avg<<"\n";
    }
}*/

//