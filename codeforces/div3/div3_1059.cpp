#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

// A - Beautiful Average 
/*int32_t main(){
    ll _{0};cin>>_;
    while(_--){
        ll n{0};cin>>n;
        ll avg{-1};
        vector<ll> a(n,0);
        for(ll i{0};i<n;++i){
            cin>>a[i];
            avg=max(avg,a[i]);
        }
        cout<<avg<<"\n";
    }
}*/

//