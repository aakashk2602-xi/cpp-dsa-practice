#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

// A - Social Experiment
/*int32_t main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
 
    ll _{0};
    cin >> _;
    while (_--) {
        ll n{0};
        cin>> n;
        if(n<=3) cout<< n;
        else cout<< n%2;
        cout<< "\n";
    }
    return 0;
}*/

// B - Hourglass
/*int32_t main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    ll _{0};
    cin >> _;
    while (_--) {
        int32_t s{0},k{0},m{0};
        cin>> s >> k >> m;

        if (s <= k) cout << max(0, s - m % k) << '\n';
        else cout << (((m % (2 * k)) < k) ? s - m % k : k - m % k) << '\n';
    }
    return 0;
}*/

// C - Huge Pile
int32_t main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
 
    ll _{0};
    cin >> _;
    while (_--) {
        
    }
    return 0;
}